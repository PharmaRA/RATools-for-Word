本文件记录 RATools for Word 值得关注的版本变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，并遵循[语义化版本](https://semver.org/lang/zh-CN/)。为降低个人维护成本，内容保持轻量。

开发中的改动先记入 `# [Unreleased]`，发版时再改为对应的版本号小节。

# [Unreleased]

# v0.8.3

- 功能区（Ribbon）「模板」组原「点击加载」按钮升级为拆分下拉菜单（SplitButton）：支持一键直接加载默认模板，或通过下拉菜单自由选择「加载中文模板 (CN)」、「加载英文模板 (EN)」与「浏览其他模板...」，免去中英文模板共存时的弹窗询问，大幅提升中英文申报排版切换效率。`✨ 新增` (Added)
- 重构模块命名规范（前缀语义分层法）：将原本统一以 `Mod_` 开头的 27 个标准模块按职责分层重命名为 `Core_*`（底层工具）、`Engine_*`（核心引擎）、`UI_*`（界面与调度）、`Macro_*`（业务宏）四大类，在 Word VBA 编辑器与文件树中天然按角色聚合，大幅提升可读性与维护性。`🛠 优化` (Changed)
- 更新 README 中「进阶用法」章节：适配重构后的宏注册机制与构建说明。`🛠 优化` (Changed)

# v0.8.2

- 宏「Word批量转PDF」新增「所见即所得」转换选项：一键锁定全文档所有故事区及文本框/图形内的全部域（等同于全选后锁定域），彻底避免导出 PDF 和重新分页时刷新目录、编号或交叉引用；单文档导出后自动解锁恢复，批量模式保留原文档完整性。`✨ 新增` (Added)
- 优化「Word批量转PDF」选项交互：提供「所见即所得（推荐）」、「仅刷新页码」与「刷新整个目录」三种选项，默认直接回车即可快速所见即所得导出。`🛠 优化` (Changed)
- 新增 `tests/BatchConvertWordToPDF.Tests.ps1` 自动化测试套件，覆盖源码静态契约与 Word COM 真实跨故事区域锁定/解锁验证。`🛠 优化` (Changed)

# v0.8.1

- 修复鼠标移到快捷悬浮窗图标上时，提示气泡被悬浮窗自身压住、显示在其层级下面的问题。悬浮窗钉入系统置顶层后，MSForms 提示气泡是独立窗口（F3 Tooltip）且不在置顶带内；改用窗体与按钮的鼠标悬停事件驱动，把气泡同步抬回置顶带。`🐛 修复` (Fixed)
- 修复快捷悬浮窗打开或悬停时 Word 卡死的问题：持续枚举全部窗口并在枚举中修改 Z 序（`SetWindowPos` 置顶）会打乱 `FindWindowEx` 的继续枚举，导致同一窗口被重复访问形成死循环；现改为找到气泡立即抬升并退出，并限制扫描上限。`🐛 修复` (Fixed)
- 移除悬浮窗中的「格式刷」按钮`🛠 优化` (Changed)
- 修复点击快捷悬浮窗按钮后，需要在正文点击两次光标才移动到目标位置的问题。点击窗体会使 Word 失活，第一次点击只是把焦点还给 Word；现在每个动作结束后主动把焦点交还文档窗口。`🐛 修复` (Fixed)
- 快捷悬浮窗不再随单个文档窗口的生命周期销毁：关闭当前文档不会连带关闭悬浮窗，切换或激活文档后自动抬回前面，Word 退出前保存窗口位置。`🛠 优化` (Changed)
- 修复点击快捷悬浮窗按钮后悬浮窗沉到 Word 后面、无法继续点击的问题。悬浮窗脱离文档窗口后不再由 Word 维持层级，现改为钉入系统置顶层，并在交还焦点、切换或激活文档后重新确认置顶。`🐛 修复` (Fixed)
- 修复 `scripts/RATools.Build.psm1` 等脚本缺少 UTF-8 BOM，在中文（GBK）系统的 Windows PowerShell 5.1 下中文注释误解码并吞掉换行、导致版本号写入等构建步骤报错的问题。`🐛 修复` (Fixed)

# v0.8.0

- 修复宏「一键表格自动调整」在文档无表格时导致 Word 界面停止刷新的问题。`🐛 修复` (Fixed)
- 修复「加载模板」在样式模板被占用或损坏时 Word 卡在等待状态的问题；「标题自动编号转文本」「超链接和域批量设置为蓝色」补充同类错误保护。`🐛 修复` (Fixed)
- 修复宏「批量接受修订并删除批注」在部分文件无法打开时完成提示虚报成功数量的问题，现区分成功与失败数。`🐛 修复` (Fixed)
- 修复宏「一键设置页边距」文件夹模式误处理 .dot/.dotx/.dotm 模板文件且不遍历子文件夹的问题，行为与其他批量宏统一。`🐛 修复` (Fixed)
- 修复 README 中 Releases 下载链接与 CHANGELOG 链接指向失效仓库的问题。`🐛 修复` (Fixed)
- 修复中文样式「标题左对齐」在英文模板下因映射拼写错误（THeading Left-F）从未生效的问题。`🐛 修复` (Fixed)
- 内部重构：拆分 mRibbon 上帝模块为样式引擎/文档命令/宏注册表；新增 Mod_Core\_* 公共层与样式名单一事实源 Mod_StyleNames；悬浮窗动作分发合并为单表，19 按钮定义收敛为 QuickToolbarButtons.psd1 单源。功能与界面行为保持不变。`🛠 优化` (Changed)
- 测试体系从 4 个套件扩至 10 个：新增统一测试入口 Run-AllTests.ps1、全工程编译探针、Ribbon 回调完整性校验、样式映射与模板一致性校验、版本一致性校验及 UpdateChecker/批量重命名纯逻辑用例；CI 在 push/PR 与发布前运行无 COM 测试并校验 tag 与源码版本一致。`🛠 优化` (Changed)

# v0.7.1

- 将快捷悬浮窗扩展为 19 个分组多列按钮，新增编号标题 1-4、无编号标题 1-4、格式刷、段落设置、表标题、图标题、插入交叉引用、更新域及三个常用批处理宏入口；继续复用 Ribbon 自定义图标和 Word 内置图标。`🛠 优化` (Changed)
- 重绘 Ribbon「快捷悬浮窗」入口图标。`🛠 优化` (Changed)

# v0.7.0

- 新增可拖动的快捷悬浮窗，v0.7.0 提供标题 1-3、正文、文字设为蓝色、段前分页、表格自动适应窗口及科学术语下标标准化，并可从 Ribbon 显示或隐藏。`✨ 新增` (Added)
- 进一步修复宏「批量检测高亮内容」因检测异常沿用上一文件结果，以及段落标记、空白和隐藏文字带高亮格式而产生的误报；批量报告新增命中位置与文本摘要，并支持跳转到第一处可见高亮。`🐛 修复` (Fixed)

# v0.6.5

- 修复宏「批量检测高亮内容」在文档没有高亮时因混合格式状态导致的误报，并支持从检测结果中跳转到第一处高亮。`🐛 修复` (Fixed)
- 扩充宏「标准化科学术语」的常用药理学、毒理学及药代动力学术语，新增对 IC50、EC50、ED50、LD50、Ki、Kd、Emax、Vss、CLr、MRTinf 等写法的下标格式化支持。`🛠 优化` (Changed)

# v0.6.4

- 新增宏「标准化科学术语」，可将常见科学术语和药代动力学术语中的后缀自动设置为真正的 Word 下标格式。`✨ 新增` (Added)
- 新增本地 `.dotm` 自动构建脚本，可从 `modules/`、`class_modules/`、`userforms/` 源码导入生成 `.dotm`，并同步回 `dotm/`。`✨ 新增` (Added)
- 新增可选 `pre-commit` hook，用于提交前自动构建并暂存 `dotm/`，减少手动保存、解包和同步步骤。`✨ 新增` (Added)
- 构建产物会自动清理作者、最后修改者、公司等文档元数据，避免 Release 文件携带个人信息。`🐛 修复` (Fixed)


# v0.6.3

- 重构宏「Word批量转PDF」，修复导出 PDF 会丢失页眉/页脚中的边框线的bug，优化操作逻辑。`🛠 优化` (Changed)

# v0.6.2

- 在「引用」项下新增“恢复题注标签”按钮，扫描文档中的 SEQ 域并补齐缺失的题注标签，便于在“交叉引用”中使用。✨ 新增 (Added)

# v0.6.1

- 新增关于对话框，集中展示版本信息与仓库入口。`✨ 新增` (Added)
- 将检查更新功能迁移到关于对话框中，减少功能区分散。`🛠 优化` (Changed)

# v0.6.0

- 新增“检查更新”按钮，可手动检查 GitHub Releases 上是否有新版本。更新提醒支持在发现新版本后选择前往 GitHub 或 Gitee 的 Release 详情页。`🛠 新增` (Added)
- 新增独立的“关于”入口，用于展示版本信息和仓库链接。`✨ 新增` (Added)
- 将“检查更新”入口从「其他」迁移到关于对话框中。`🛠 优化` (Changed)

# v0.5.11

- 调整「重命名当前文件」功能入口，现移至功能区「其他」项下。`🛠 优化` (Changed)
- 补充README项目结构说明，明确源码导出与同步约定。`🛠 优化` (Changed)

# v0.5.10

- 优化宏「Word批量转PDF」，导出当前文档时，新增“仅刷新目录页码”支持。`🛠 优化` (Changed)
- 调整「域格式保护」交互，提取为独立功能，现移至功能区「其他」项下。`🛠 优化` (Changed)

# v0.5.9

- 新增按钮，打开当前文件所在文件夹。`✨ 新增` (Added)
- 修复宏「批量接受修订并删除批注」未能删除全部批注的问题。`🐛 修复` (Fixed)

# v0.5.8

- 新增宏，一键提取全大写英文缩略语。`✨ 新增` (Added)

# v0.5.7

- 优化样式附加逻辑，加快加载速度。`🛠 优化` (Changed)
- 新增宏「批量检测高亮内容」，检测文档是否有突出显示颜色的内容。`✨ 新增` (Added)
- 新增宏「清理未使用的模板样式」。`✨ 新增` (Added)

# v0.5.6

## RATools.dotm

- 优化样式附加的逻辑。`🛠 优化` (Changed)
- 修复附加模版时，点击取消后，仍然弹出「未找到默认样式模板」提示的问题。`🐛 修复` (Fixed)

## master-template-en.dotx

- 「Body Text with Indentation-F」样式的缩进调整为首行缩进2字符。`🐛 修复` (Fixed)

# v0.5.5

- 新增宏，将所有页眉和页脚设置为“链接到前一节”。`✨ 新增` (Added)
- 优化宏「Word批量转PDF」。`🛠 优化` (Changed)
- 修改字体颜色调整的方式，由应用样式的方式调整为直接修改。`🛠 优化` (Changed)

# v0.5.4

- 通过Windows API实现窗体拖拽，现在可以自由拖拽宏管理器大小。`🛠 优化` (Changed)

# v0.5.3

- 现在新建和打开word文档时会默认打开RATools选项卡。`🛠 优化` (Changed)

# v0.5.2

- 优化了宏管理器的使用。`🛠 优化` (Changed)
- 新增宏，批量将tracking版转换为clean版，接受所有修订并停止修订同时删除文档中的所有批注。`✨ 新增` (Added)

# v0.5.1

- 修复「根据窗口自动调整表格」按钮不生效的问题。`🐛 修复` (Fixed)
- 「字体和段落」项下加入「段前分页」按钮。`✨ 新增` (Added)
- 为宏列表添加搜索功能。`✨ 新增` (Added)
- 优化宏「批量修改文件名」。`🛠 优化` (Changed)

# v0.5.0

## RATools.dotm

- 优化获取模板文件路径的方法，现在只需要将`.dotx`和`.dotm`放在同一路径下即可进行模板样式附加。`🛠 优化` (Changed)
- 重构样式附加的逻辑，实现中英文模板样式的附加。`🛠 优化` (Changed)

## master-template-cn.dotx

- 部分样式进行了优化。`🛠 优化` (Changed)

## master-template-en.dotx

- 添加了英文模板。`✨ 新增` (Added)

# v0.4.5

- 新增宏，批量设置页边距，将单个或多个文件页面上、下、左、右的页边距设置为 2.54厘米。`✨ 新增` (Added)
- 「表格」项下加入「根据窗口自动调整表格」按钮。`✨ 新增` (Added)
- 「字体和段落」项下添加「字号」「行距」等按钮。`✨ 新增` (Added)
- 优化宏「重命名当前文件」。`🛠 优化` (Changed)

# v0.4.4

- 新增宏，修改当前已打开文件的文件名。`✨ 新增` (Added)
- 新增宏，批量调整表格，将文档中所有表格批量设置为“根据窗口自动调整”。
- 修复宏「超链接一键蓝字」会将页码也标蓝的问题。`🐛 修复` (Fixed)
- 优化 「域格式保护」的实现。`🛠 优化` (Changed)
- 优化宏「一键批量修改文件名」，现在将大写字母转为小写。`🛠 优化` (Changed)
- 优化宏「Word批量转PDF」，现在在转换前会自动刷新目录和图表目录，并且会输出转换报告。`🛠 优化` (Changed)

# v0.4.3

- 对Logo进行了一些调整。`🛠 优化` (Changed)
- 优化样式复制时出现的一些问题，同时复制更多样式，用于创建目录以及图表目录。`🛠 优化` (Changed)
- 新增宏，将文档中所有标题（大纲 1-9 级）的自动编号转换为固定的静态文本，也许有时会有帮助。`✨ 新增` (Added)

# v0.4.2

## RATools.dotm

- 调整样式加载方式，从原有的附加模板变更为通过管理器复制后缀为「-F」的样式，避免对文件原有样式的覆盖。`🛠 优化` (Changed)
- 新增宏，用于批量调整文件名，使其符合eCTD对文件命名的规范要求。`✨ 新增` (Added)

## master-template-cn.dotx

- 精简无用的样式。`🛠 优化` (Changed)

# v0.4.0

- 新增宏管理器，方便用户更加便捷地进行宏的管理。`✨ 新增` (Added)
- 移除原界面的宏命令按钮，相关功能已整合至宏列表中。`🗑 移除` (Removed)
- 将「常用宏」选项卡更名为「宏列表」，使界面更清晰。`🛠 优化` (Changed)
- 修复「超链接蓝色」样式在部分情况下失效的问题。`🐛 修复` (Fixed)

# v0.3.19

- 首个基本可用版本。 `✨ 新增` (Added)

