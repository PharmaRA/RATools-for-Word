# Unreleased

- 进一步修复宏「批量检测高亮内容」因检测异常沿用上一文件结果，以及段落标记、空白和隐藏文字带高亮格式而产生的误报；批量报告新增命中位置与文本摘要，并支持跳转到第一处可见高亮。`🐛 修复` (Fixed)

# v0.6.5

- 修复宏「批量检测高亮内容」在文档没有高亮时因混合格式状态导致的误报，并支持从检测结果中跳转到第一处高亮。`🐛 修复` (Fixed)
- 扩充宏「标准化科学术语」的常用药理学、毒理学及药代动力学术语，新增对 IC50、EC50、ED50、LD50、Ki、Kd、Emax、Vss、CLr、MRTinf 等写法的下标格式化支持。`🛠 优化` (Changed)

# v0.6.4

- 新增宏「标准化科学术语」，可将常见科学术语和药代动力学术语中的后缀自动设置为真正的 Word 下标格式。`✨ 新增` (Added)
- 新增本地 `.dotm` 自动构建脚本，可从 `modules/`、`class_modules/`、`userforms/` 源码导入生成 `.dotm`，并同步回 `dotm/`。`✨ 新增` (Added)
- 新增可选 `pre-commit` hook，用于提交前自动构建并暂存 `dotm/`，减少手动保存、解包和同步步骤。`✨ 新增` (Added)
- 构建产物会自动清理作者、最后修改者、公司等文档元数据，避免 Release 文件携带个人信息。`🐛 修复` (Fixed)


# v0.6.3

- 重构宏「Word批量转PDF」，修复导出 PDF 会丢失页眉/页脚中的边框线的bug，优化操作逻辑。`🛠 优化` (Changed)

# v0.6.2

- 在「引用」项下新增“恢复题注标签”按钮，扫描文档中的 SEQ 域并补齐缺失的题注标签，便于在“交叉引用”中使用。✨ 新增 (Added)

# v0.6.1

- 新增关于对话框，集中展示版本信息与仓库入口。`✨ 新增` (Added)
- 将检查更新功能迁移到关于对话框中，减少功能区分散。`🛠 优化` (Changed)

# v0.6.0

- 新增“检查更新”按钮，可手动检查 GitHub Releases 上是否有新版本。更新提醒支持在发现新版本后选择前往 GitHub 或 Gitee 的 Release 详情页。`🛠 新增` (Added)
- 新增独立的“关于”入口，用于展示版本信息和仓库链接。`✨ 新增` (Added)
- 将“检查更新”入口从「其他」迁移到关于对话框中。`🛠 优化` (Changed)

# v0.5.11

- 调整「重命名当前文件」功能入口，现移至功能区「其他」项下。`🛠 优化` (Changed)
- 补充README项目结构说明，明确源码导出与同步约定。`🛠 优化` (Changed)

# v0.5.10

- 优化宏「Word批量转PDF」，导出当前文档时，新增“仅刷新目录页码”支持。`🛠 优化` (Changed)
- 调整「域格式保护」交互，提取为独立功能，现移至功能区「其他」项下。`🛠 优化` (Changed)

# v0.5.9

- 新增按钮，打开当前文件所在文件夹。`✨ 新增` (Added)
- 修复宏「批量接受修订并删除批注」未能删除全部批注的问题。`🐛 修复` (Fixed)

# v0.5.8

- 新增宏，一键提取全大写英文缩略语。`✨ 新增` (Added)

# v0.5.7

- 优化样式附加逻辑，加快加载速度。`🛠 优化` (Changed)
- 新增宏「批量检测高亮内容」，检测文档是否有突出显示颜色的内容。`✨ 新增` (Added)
- 新增宏「清理未使用的模板样式」。`✨ 新增` (Added)

# v0.5.6

## RATools.dotm

- 优化样式附加的逻辑。`🛠 优化` (Changed)
- 修复附加模版时，点击取消后，仍然弹出「未找到默认样式模板」提示的问题。`🐛 修复` (Fixed)

## master-template-en.dotx

- 「Body Text with Indentation-F」样式的缩进调整为首行缩进2字符。`🐛 修复` (Fixed)

# v0.5.5

- 新增宏，将所有页眉和页脚设置为“链接到前一节”。`✨ 新增` (Added)
- 优化宏「Word批量转PDF」。`🛠 优化` (Changed)
- 修改字体颜色调整的方式，由应用样式的方式调整为直接修改。`🛠 优化` (Changed)

# v0.5.4

- 通过Windows API实现窗体拖拽，现在可以自由拖拽宏管理器大小。`🛠 优化` (Changed)

# v0.5.3

- 现在新建和打开word文档时会默认打开RATools选项卡。`🛠 优化` (Changed)

# v0.5.2

- 优化了宏管理器的使用。`🛠 优化` (Changed)
- 新增宏，批量将tracking版转换为clean版，接受所有修订并停止修订同时删除文档中的所有批注。`✨ 新增` (Added)

# v0.5.1

- 修复「根据窗口自动调整表格」按钮不生效的问题。`🐛 修复` (Fixed)
- 「字体和段落」项下加入「段前分页」按钮。`✨ 新增` (Added)
- 为宏列表添加搜索功能。`✨ 新增` (Added)
- 优化宏「批量修改文件名」。`🛠 优化` (Changed)

# v0.5.0

## RATools.dotm

- 优化获取模板文件路径的方法，现在只需要将`.dotx`和`.dotm`放在同一路径下即可进行模板样式附加。`🛠 优化` (Changed)
- 重构样式附加的逻辑，实现中英文模板样式的附加。`🛠 优化` (Changed)

## master-template-cn.dotx

- 部分样式进行了优化。`🛠 优化` (Changed)

## master-template-en.dotx

- 添加了英文模板。`✨ 新增` (Added)

# v0.4.5

- 新增宏，批量设置页边距，将单个或多个文件页面上、下、左、右的页边距设置为 2.54厘米。`✨ 新增` (Added)
- 「表格」项下加入「根据窗口自动调整表格」按钮。`✨ 新增` (Added)
- 「字体和段落」项下添加「字号」「行距」等按钮。`✨ 新增` (Added)
- 优化宏「重命名当前文件」。`🛠 优化` (Changed)

# v0.4.4

- 新增宏，修改当前已打开文件的文件名。`✨ 新增` (Added)
- 新增宏，批量调整表格，将文档中所有表格批量设置为“根据窗口自动调整”。
- 修复宏「超链接一键蓝字」会将页码也标蓝的问题。`🐛 修复` (Fixed)
- 优化 「域格式保护」的实现。`🛠 优化` (Changed)
- 优化宏「一键批量修改文件名」，现在将大写字母转为小写。`🛠 优化` (Changed)
- 优化宏「Word批量转PDF」，现在在转换前会自动刷新目录和图表目录，并且会输出转换报告。`🛠 优化` (Changed)

# v0.4.3

- 对Logo进行了一些调整。`🛠 优化` (Changed)
- 优化样式复制时出现的一些问题，同时复制更多样式，用于创建目录以及图表目录。`🛠 优化` (Changed)
- 新增宏，将文档中所有标题（大纲 1-9 级）的自动编号转换为固定的静态文本，也许有时会有帮助。`✨ 新增` (Added)

# v0.4.2

## RATools.dotm

- 调整样式加载方式，从原有的附加模板变更为通过管理器复制后缀为「-F」的样式，避免对文件原有样式的覆盖。`🛠 优化` (Changed)
- 新增宏，用于批量调整文件名，使其符合eCTD对文件命名的规范要求。`✨ 新增` (Added)

## master-template-cn.dotx

- 精简无用的样式。`🛠 优化` (Changed)

# v0.4.0

- 新增宏管理器，方便用户更加便捷地进行宏的管理。`✨ 新增` (Added)
- 移除原界面的宏命令按钮，相关功能已整合至宏列表中。`🗑 移除` (Removed)
- 将「常用宏」选项卡更名为「宏列表」，使界面更清晰。`🛠 优化` (Changed)
- 修复「超链接蓝色」样式在部分情况下失效的问题。`🐛 修复` (Fixed)

# v0.3.19

- 首个基本可用版本。 `✨ 新增` (Added) 

